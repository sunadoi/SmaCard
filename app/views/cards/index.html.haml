.container
  .contents
    - @cards.each do |card|
      = render partial: "card", locals: { card: card }
    - if user_signed_in?
      %p 現在のログインユーザー: #{current_user.name}
    - if admin_signed_in?
      %p 現在のログイン管理者: #{current_admin.name}
